<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Java2.mapper.OrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.Java2.pojo.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="deliver_addr_id" property="deliverAddrId"/>
        <result column="goods_name" property="goodsName"/>
        <result column="goods_count" property="goodsCount"/>
        <result column="order_channel" property="orderChannel"/>
        <result column="status" property="status"/>
        <result column="creadate_date" property="creadateDate"/>
        <result column="pay_date" property="payDate"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, goods_id, deliver_addr_id, goods_name, goods_count, order_channel, status, creadate_date, pay_date
    </sql>

</mapper>
